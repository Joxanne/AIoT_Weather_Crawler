# Implementation Tasks

- [ ] Import `argparse` library
- [ ] Define arguments: `--db` (default: `sqlitedata.db`), `--verbose` (flag)
- [ ] Update `init_db` and `save_to_db` to accept database path
- [ ] Update `main` to parse arguments and pass them to functions
- [ ] Verify script runs with default arguments
- [ ] Verify script runs with custom arguments
